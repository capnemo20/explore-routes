/*
 1. install react router: npm i react-router-dom
 2. create a router using createBrowserRouter
 3. add RouterProvider and pass router props
 4. create some routes inside the router
 */

 /* 
 Things you need to create a route

 1.create a <Link> element so the you can go to that route
 2. create a component to show what you'll display when you go to that route
 3. add route so that you can route to that component
 
 */